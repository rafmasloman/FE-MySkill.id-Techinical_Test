import { Group, Image } from '@mantine/core';

const Navbar = () => {
  return (
    <div>
      <Image src={''} alt="Logo Image" />
      <Group></Group>
    </div>
  );
};

export default Navbar;
